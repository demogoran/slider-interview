enum ErrorsEnum {
  GetImagesList = "Can't fetch image list",
  GetImageList = "Can't fetch image",
  PostImagesList = "Can't create image",
  PutImagesList = "Can't update image",
  DeletemagesList = "Can't delete image list",
}

export { ErrorsEnum };
